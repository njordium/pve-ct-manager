# pve-ct-manager
Interactive Bash manager for Proxmox VE LXC containers across a cluster. Renumber CTs, change hostname/VLAN/IP, and migrate between nodes from any host — with pre-flight orphan detection, post-rename LV verification, nesting-aware migration timing, automatic source cleanup, egress IP diagnostics, and rollback snippets for every change.
